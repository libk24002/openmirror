apiVersion: v1
kind: Service
metadata:
  name: openmirror
  labels:
    app: openmirror
spec:
  type: ClusterIP
  selector:
    app: openmirror
  ports:
    - name: http
      port: 80
      targetPort: 8080
